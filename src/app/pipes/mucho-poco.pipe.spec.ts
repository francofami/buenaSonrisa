import { MuchoPocoPipe } from './mucho-poco.pipe';

describe('MuchoPocoPipe', () => {
  it('create an instance', () => {
    const pipe = new MuchoPocoPipe();
    expect(pipe).toBeTruthy();
  });
});
